language: java
jdk: openjdk8
install: true

cache:
  directories:
    - $HOME/.m2

jobs:
  include:
    - stage : build
      script: ./mvnw clean install -Dpact.verifier.publishResults=true

branches:
  only:
    - master